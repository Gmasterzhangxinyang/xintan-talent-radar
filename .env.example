# Hosted Worker settings. Keep real values in the deployment secret store.
COMPUTER_AGENT_URL=
COMPUTER_AGENT_TOKEN=
COMPUTER_AGENT_CALLBACK_SECRET=
SCHEDULER_SECRET=

# Local assistant target. The AI key is configured in the local AI Center and
# is written only to the ignored work/ directory with owner-only permissions.
XINTAN_APP_URL=http://localhost:3000
